Not implemented yet. Planned: `ci.yml` (pytest + ruff + mypy on `backend/`,
`prove` on `agent-perl/`), `security.yml` (bandit, pip-audit), `quality.yml`
(YAML rule schema validation, demo scan/report generation). See
[../../ROADMAP.md](../../ROADMAP.md).
