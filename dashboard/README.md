# Dashboard (optional)

Not implemented in this iteration by design — see the "Scope decisions" in
[../ROADMAP.md](../ROADMAP.md). The HTML report generated by
`backend/app/reports` is the primary deliverable; a Streamlit or Next.js
dashboard can be layered on top of the backend API later without changes
to the rules engine or domain layer.
