# Fichier de config de l'app, déployé en même temps que le code. Dans un
# vrai déploiement, ce fichier traînerait avec des permissions bien trop
# larges (664, voire 666) puisque tout le monde sur le serveur doit
# pouvoir le relire — encore un classique du legacy mal géré.
#
# ATTENTION : secrets volontairement FAUX, juste pour la démo du scanner.

db_host = localhost
db_user = legacy_app
password = "hunter2super"

smtp_host = mail.internal
smtp_api_key = "AKIAFAKEEXAMPLE12345"
